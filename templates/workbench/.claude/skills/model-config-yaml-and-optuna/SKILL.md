---
name: model-config-yaml-and-optuna
description: Gestiona configuración de variables, monotonicidades, hiperparámetros y optimización en YAML
---

Mantén:
- YAML editable de hiperparámetros;
- YAML editable de variables y monotonicidades;
- YAML o CSV con todas las variables candidatas y su estado.

Integra Optuna cuando la tarea lo requiera.
